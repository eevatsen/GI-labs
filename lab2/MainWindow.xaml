<Window x:Class="TextEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TextEditor" Height="450" Width="600">
    <Window.Resources>
        <!-- ресурс градієнта -->
        <LinearGradientBrush x:Key="LinearPanelBrush">
            <GradientStop Color="Yellow" Offset="0"/>
            <GradientStop Color="Cyan" Offset="0.5"/>
            <GradientStop Color="Yellow" Offset="1"/>
        </LinearGradientBrush>

        <!-- стиль кнопок -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="White"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9*"/>
            <ColumnDefinition Width="191*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10" Background="{StaticResource LinearPanelBrush}" Grid.ColumnSpan="2">
            <StackPanel Orientation="Horizontal" FocusManager.IsFocusScope="True">
                <Button ToolTip="Відкрити" Command="Open">
                    <Image Source="images/open.png" Width="32" Height="32"/>
                </Button>
                <Button ToolTip="Стерти" Command="Delete" CommandTarget="{Binding ElementName=txtDocument}">
                    <Image Source="images/clear.png" Width="32" Height="32"/>
                </Button>
                <Button ToolTip="Копіювати" Command="Copy" CommandTarget="{Binding ElementName=txtDocument}">
                    <Image Source="images/copy.png" Width="32" Height="32"/>
                </Button>
                <Button ToolTip="Вставити" Command="Paste" CommandTarget="{Binding ElementName=txtDocument}">
                    <Image Source="images/paste.png" Width="32" Height="32"/>
                </Button>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" BorderBrush="#FFCBBEBE" BorderThickness="2" CornerRadius="10" Background="{StaticResource LinearPanelBrush}" Grid.ColumnSpan="2">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBox x:Name="txtDocument" Grid.Row="0" Margin="5"
                         VerticalScrollBarVisibility="Auto" AcceptsReturn="True"
                         FontSize="{Binding ElementName=fontSlider, Path=Value, UpdateSourceTrigger=PropertyChanged}"/>

                <DockPanel Grid.Row="1" Margin="5">
                    <Label DockPanel.Dock="Left">Розмір шрифту:</Label>
                    <Slider x:Name="fontSlider" DockPanel.Dock="Left" Margin="4" MinWidth="100" Maximum="100" Minimum="12" Value="12"/>
                    <Button DockPanel.Dock="Right" HorizontalAlignment="Right" Style="{x:Null}" Width="40" Command="Save">ОК</Button>
                </DockPanel>
            </Grid>
        </Border>
    </Grid>
</Window>

